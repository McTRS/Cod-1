print('digite apenas numeros inteiros positivos')
lado1 = int(input('maior lado do triangulo: '))
lado2 = int(input('lado 2 do triangulo: '))
lado3 = int(input('lado 3 do triangulo: '))

while lado1 != 0 and lado2 != 0 and lado3 != 0:
    if (lado1 < lado2 + lado3):
        s = (lado1 + lado2 + lado3)/2
        a = (s*(s - lado1)* (s - lado2) *(s- lado3))**(0.5)
        print('área = ', a)
        break
